package com.atman.jixin.model.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.atman.jixin.model.bean.LocationNumberModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOCATION_NUMBER_MODEL".
*/
public class LocationNumberModelDao extends AbstractDao<LocationNumberModel, Long> {

    public static final String TABLENAME = "LOCATION_NUMBER_MODEL";

    /**
     * Properties of entity LocationNumberModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property TargetId = new Property(1, long.class, "targetId", false, "TARGET_ID");
        public final static Property LoginId = new Property(2, long.class, "loginId", false, "LOGIN_ID");
        public final static Property Location = new Property(3, String.class, "location", false, "LOCATION");
    }


    public LocationNumberModelDao(DaoConfig config) {
        super(config);
    }
    
    public LocationNumberModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOCATION_NUMBER_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"TARGET_ID\" INTEGER NOT NULL ," + // 1: targetId
                "\"LOGIN_ID\" INTEGER NOT NULL ," + // 2: loginId
                "\"LOCATION\" TEXT);"); // 3: location
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOCATION_NUMBER_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LocationNumberModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getTargetId());
        stmt.bindLong(3, entity.getLoginId());
 
        String location = entity.getLocation();
        if (location != null) {
            stmt.bindString(4, location);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LocationNumberModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getTargetId());
        stmt.bindLong(3, entity.getLoginId());
 
        String location = entity.getLocation();
        if (location != null) {
            stmt.bindString(4, location);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LocationNumberModel readEntity(Cursor cursor, int offset) {
        LocationNumberModel entity = new LocationNumberModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // targetId
            cursor.getLong(offset + 2), // loginId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // location
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LocationNumberModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTargetId(cursor.getLong(offset + 1));
        entity.setLoginId(cursor.getLong(offset + 2));
        entity.setLocation(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LocationNumberModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LocationNumberModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LocationNumberModel entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
